% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdtm_swimlane_data.R
\name{sdtm_swimlane_data}
\alias{sdtm_swimlane_data}
\title{Derive swimlane plot data from SDTM datasets}
\usage{
sdtm_swimlane_data(
  dm,
  rs = NULL,
  evaluator = "INVESTIGATOR",
  time_unit = c("days", "weeks", "months")
)
}
\arguments{
\item{dm}{SDTM DM (Demographics) data frame, e.g. \code{pharmaversesdtm::dm}.}

\item{rs}{Optional SDTM RS (Disease Response) data frame, e.g.
\code{pharmaversesdtm::rs_onco}. When given, overall response assessments
(\code{RSTESTCD == "OVRLRESP"}) become events and only subjects with
response records are kept.}

\item{evaluator}{Value of \code{RSEVAL} to use, such as \code{"INVESTIGATOR"} or
\code{"INDEPENDENT ASSESSOR"}. Where an evaluator has several readers, only
records flagged as accepted (\code{RSACPTFL == "Y"}) are kept.}

\item{time_unit}{Unit for \code{DURATION} and \code{TIME}: \code{"days"}, \code{"weeks"}, or
\code{"months"} (30.4375 days).}
}
\value{
A data frame with columns \code{USUBJID}, \code{ACTARM}, \code{DURATION} (time on
treatment), \code{TIME} (time of the event), and \code{EVENT} (a factor of
response categories plus \code{"Death"}). Subjects without events have one
row with \code{TIME} and \code{EVENT} set to \code{NA}.
}
\description{
Turns SDTM demographics (DM) and, optionally, disease response (RS) into
the long format used by \code{\link[=swimlane_plot]{swimlane_plot()}}: each subject's time on
treatment plus one row per response assessment and death.
}
\details{
Time on treatment runs from first to last dose (\code{RFXSTDTC} to \code{RFXENDTC}).
Event times use the SDTM study day convention relative to first dose:
day 1 is the day of first dose and there is no day 0. Records with
missing or partial dates are dropped with a message.
}
\examples{
\dontshow{if (requireNamespace("pharmaversesdtm", quietly = TRUE)) withAutoprint(\{ # examplesIf}
sw <- sdtm_swimlane_data(
  pharmaversesdtm::dm,
  rs = pharmaversesdtm::rs_onco,
  time_unit = "weeks"
)
head(sw)

# Swimlane plots usually show responders only.
responders <- unique(sw$USUBJID[sw$EVENT \%in\% c("CR", "PR")])
swimlane_plot(
  sw[sw$USUBJID \%in\% responders[1:20], ],
  USUBJID,
  DURATION,
  TIME,
  EVENT,
  fill = ACTARM,
  xlab = "Weeks since first dose"
)
\dontshow{\}) # examplesIf}
}
