% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waterfall.R
\name{prepare_waterfall}
\alias{prepare_waterfall}
\title{Prepare waterfall data}
\usage{
prepare_waterfall(
  data,
  x_var,
  value_var,
  sort_var = NULL,
  color_var = NULL,
  hlines = NULL,
  title = NULL
)
}
\arguments{
\item{data}{A data frame, one row per subject.}

\item{x_var}{Column identifying the subject. One bar each.}

\item{value_var}{Numeric column giving the bar height.}

\item{sort_var}{Column to sort by. The default, \code{NULL}, sorts by
\code{value_var} descending, because a waterfall that is not sorted by value is
not a waterfall. Pass \code{FALSE} to keep the order of the input.}

\item{color_var}{Optional column driving the fill colour, e.g. a response
category.}

\item{hlines}{Numeric vector of reference line positions. These are in the
units of \code{value_var}: for RECIST against a percentage change column that is
\code{c(20, -30)}, not \code{c(0.2, -0.3)}.}

\item{title}{Plot title, or \code{NULL} for none.}
}
\value{
An object of class \code{floaties_data_waterfall}.
}
\description{
Resolves the column arguments, validates them, sorts, and returns the
canonical structure the renderers consume. Exported so that engine authors
and tests can build a \code{floaties_data_waterfall} without drawing anything.
}
\details{
The result is a data frame with columns \code{.id} (a factor already in plot
order), \code{.value} and \code{.group}, carrying \code{hlines} and \code{title} as attributes.
}
\examples{
subjects <- data.frame(id = c("a", "b"), change = c(10, -20))
prepare_waterfall(subjects, id, change)

}
