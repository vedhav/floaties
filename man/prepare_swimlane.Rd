% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swimlane.R
\name{prepare_swimlane}
\alias{prepare_swimlane}
\title{Prepare swimlane data}
\usage{
prepare_swimlane(
  data,
  id_var,
  start_var,
  end_var = NULL,
  type_var = NULL,
  sort_lanes = c("duration", "id", "input"),
  vlines = NULL,
  title = NULL
)
}
\arguments{
\item{data}{A data frame, one row per interval or event.}

\item{id_var}{Column identifying the subject. One lane each.}

\item{start_var}{Numeric column giving the start time, typically a study day.}

\item{end_var}{Optional numeric column giving the end time. Rows where this
is missing are drawn as point events. Omit it entirely for data that is
only events.}

\item{type_var}{Optional column naming the kind of thing each row is, e.g.
\code{"dose"} or \code{"adverse event"}. Drives colour and the legend.}

\item{sort_lanes}{How to order the lanes, top to bottom. \code{"duration"}, the
default, puts the longest time on study at the top; \code{"id"} sorts
alphabetically; \code{"input"} keeps the order of first appearance.}

\item{vlines}{Numeric vector of reference line positions, in the units of
\code{start_var}.}

\item{title}{Plot title, or \code{NULL} for none.}
}
\value{
An object of class \code{floaties_data_swimlane}.
}
\description{
Resolves the column arguments, validates them, orders the lanes, and returns
the canonical structure the renderers consume.
}
\details{
The result is a data frame with columns \code{.id} (a factor whose level order is
the lane order, top to bottom), \code{.start}, \code{.end} (\code{NA} for point events) and
\code{.type}, carrying \code{vlines} and \code{title} as attributes.
}
\examples{
events <- data.frame(s = c("a", "b"), d = c(1, 3), e = c(10, NA))
prepare_swimlane(events, s, d, e)

}
