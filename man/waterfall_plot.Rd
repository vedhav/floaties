% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waterfall.R
\name{waterfall_plot}
\alias{waterfall_plot}
\alias{waterfall_ggplot}
\alias{waterfall_plotly}
\alias{waterfall_echarts4r}
\title{Waterfall plot}
\usage{
waterfall_plot(data, ..., engine = getOption("floaties.engine", "ggplot2"))

waterfall_ggplot(
  data,
  id,
  value,
  fill = NULL,
  ref_lines = c(20, -30),
  title = NULL,
  x_label = "Subject",
  y_label = "Best \% change from baseline"
)

waterfall_plotly(
  data,
  id,
  value,
  fill = NULL,
  ref_lines = c(20, -30),
  title = NULL,
  x_label = "Subject",
  y_label = "Best \% change from baseline"
)

waterfall_echarts4r(
  data,
  id,
  value,
  fill = NULL,
  ref_lines = c(20, -30),
  title = NULL,
  x_label = "Subject",
  y_label = "Best \% change from baseline"
)
}
\arguments{
\item{data}{A data frame with one row per subject.}

\item{...}{Arguments passed on to the engine's generator.}

\item{engine}{Plotting engine: one of \code{"ggplot2"}, \code{"plotly"} or
\code{"echarts4r"}. Defaults to the \code{floaties.engine} option, or \code{"ggplot2"}
if that is unset.}

\item{id}{Name of the subject identifier column.}

\item{value}{Name of the numeric column holding each subject's change.}

\item{fill}{Optional name of a column used to colour bars, such as the best
overall response.}

\item{ref_lines}{Numeric vector of y values drawn as dashed reference lines.
The default marks the RECIST thresholds for progression (+20\%) and
partial response (-30\%). Use \code{NULL} for none.}

\item{title}{Optional plot title.}

\item{x_label, y_label}{Axis titles.}
}
\value{
A ggplot object, or a plotly or echarts4r htmlwidget, depending on
the engine.
}
\description{
Draws one vertical bar per subject showing a change from baseline, such as
the best percentage change in tumour size, sorted from the largest increase
to the deepest reduction.
}
\details{
\code{waterfall_plot()} dispatches to the generator for the chosen \code{engine}. The
generators can also be called directly: \code{waterfall_ggplot()} returns a
ggplot object, \code{waterfall_plotly()} a plotly htmlwidget and
\code{waterfall_echarts4r()} an echarts4r htmlwidget. All three take the same
arguments and draw the same plot.
}
\examples{
tumour <- data.frame(
  subject = sprintf("S\%02d", 1:8),
  best_change = c(35, 12, -5, -28, -31, -45, -60, -100),
  response = c("PD", "SD", "SD", "SD", "PR", "PR", "PR", "CR")
)

waterfall_plot(tumour, id = "subject", value = "best_change", fill = "response")

if (requireNamespace("echarts4r", quietly = TRUE)) {
  waterfall_plot(
    tumour,
    id = "subject", value = "best_change", fill = "response",
    engine = "echarts4r"
  )
}
}
