% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waterfall_plot.R
\name{waterfall_plot}
\alias{waterfall_plot}
\title{Waterfall plot of best percent change from baseline}
\usage{
waterfall_plot(
  data,
  id,
  value,
  fill = NULL,
  ref_lines = c(20, -30),
  xlab = "Subject",
  ylab = "Best \% change from baseline",
  title = NULL,
  engine = c("ggplot2", "plotly", "echarts4r")
)
}
\arguments{
\item{data}{A data frame with one row per subject.}

\item{id}{<\code{\link[ggplot2:aes_eval]{data-masking}}> Column that identifies
subjects. Values must be unique.}

\item{value}{<\code{\link[ggplot2:aes_eval]{data-masking}}> Numeric column holding
the best percent change from baseline. Rows with missing values are
dropped with a warning.}

\item{fill}{<\code{\link[ggplot2:aes_eval]{data-masking}}> Optional categorical
column used to color the bars, such as best overall response or
treatment arm.}

\item{ref_lines}{Numeric vector of y values where dashed reference lines
are drawn. Use \code{NULL} for none.}

\item{xlab, ylab, title}{Axis labels and plot title.}

\item{engine}{Plotting package to use: \code{"ggplot2"}, \code{"plotly"}, or
\code{"echarts4r"}. The interactive engines need their package installed.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object for \code{engine = "ggplot2"}, otherwise a
plotly or echarts4r htmlwidget. Each can be customized further with its
own package's functions.
}
\description{
Draws a clinical waterfall plot: one bar per subject showing the best
percent change from baseline (for example, in tumor size), sorted from the
largest increase to the largest decrease. Dashed reference lines mark the
RECIST thresholds for progressive disease (+20\%) and partial response
(-30\%) by default.
}
\details{
The plot can be drawn with ggplot2 (static, the default), plotly, or
echarts4r. The interactive engines show each subject's values on hover and
use the same colours as the ggplot2 version.
}
\examples{
set.seed(1)
tumor <- data.frame(
  subject = sprintf("S\%02d", 1:30),
  pchg = round(runif(30, -100, 60)),
  arm = sample(c("Placebo", "Treatment"), 30, replace = TRUE)
)

waterfall_plot(tumor, subject, pchg)
waterfall_plot(tumor, subject, pchg, fill = arm, title = "Best response")

\dontshow{if (requireNamespace("plotly", quietly = TRUE)) withAutoprint(\{ # examplesIf}
waterfall_plot(tumor, subject, pchg, fill = arm, engine = "plotly")
\dontshow{\}) # examplesIf}
\dontshow{if (requireNamespace("echarts4r", quietly = TRUE)) withAutoprint(\{ # examplesIf}
waterfall_plot(tumor, subject, pchg, fill = arm, engine = "echarts4r")
\dontshow{\}) # examplesIf}
}
