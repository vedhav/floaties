% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waterfall.R
\name{plot_waterfall}
\alias{plot_waterfall}
\alias{prep_waterfall}
\title{Waterfall plot}
\usage{
plot_waterfall(data, ..., engine = c("ggplot2", "plotly", "echarts4r"))

prep_waterfall(
  data,
  subject,
  value,
  fill = NULL,
  tooltip = NULL,
  sort = c("descending", "ascending", "none"),
  ref_lines = c(20, -30),
  colors = NULL,
  title = NULL,
  x_label = NULL,
  y_label = NULL
)
}
\arguments{
\item{data}{A data frame with one row per subject.}

\item{...}{Arguments passed on to \code{prep_waterfall()}.}

\item{engine}{Graphics engine: \code{"ggplot2"}, \code{"plotly"}, or \code{"echarts4r"}.}

\item{subject}{<\code{\link[tidyselect:language]{tidy-select}}> Column that
identifies each subject. Must not contain missing values.}

\item{value}{<\code{\link[tidyselect:language]{tidy-select}}> Numeric column with the
bar height. Rows where it is missing are dropped with a warning.}

\item{fill}{<\code{\link[tidyselect:language]{tidy-select}}> Optional column used to
color the bars. Factor level order is kept; missing values are shown as
\code{"Missing"}.}

\item{tooltip}{<\code{\link[tidyselect:language]{tidy-select}}> Optional extra columns
shown in the tooltip, for example \code{c(age, arm)}. The subject, value and
fill are always shown. Tooltips are only used by interactive engines.}

\item{sort}{How to order the bars: \code{"descending"} (largest value on the
left, the default), \code{"ascending"}, or \code{"none"} (keep the row order of
\code{data}).}

\item{ref_lines}{Numeric vector of y values for dashed reference lines, or
\code{NULL} for none. Defaults to \code{c(20, -30)}, the RECIST 1.1 thresholds for
progression and partial response.}

\item{colors}{Optional colors for the fill levels: either a named character
vector (names are fill levels) or an unnamed one used in level order.}

\item{title}{Optional plot title.}

\item{x_label, y_label}{Optional axis titles. By default the \code{label}
attribute of the \code{subject} and \code{value} columns is used, or the column
name if there is none.}
}
\value{
\itemize{
\item \code{plot_waterfall()}: the engine's own object, a \code{ggplot} for ggplot2 or an
htmlwidget for plotly and echarts4r, which you can keep customizing.
\item \code{prep_waterfall()}: a \code{floaties_waterfall_spec} object. Its \code{data}
element has one row per subject in plot order, with columns \code{.subject},
\code{.value}, \code{.fill}, and \code{.tooltip}.
}
}
\description{
Draw a waterfall plot: one bar per subject, sorted by value. A typical use
is the best percentage change from baseline in tumor size, colored by best
overall response.

\code{plot_waterfall()} works with any data standard. \code{data} must have \strong{one row
per subject}. Filter or derive the value to plot (for example the best
percentage change) before calling it.

\code{prep_waterfall()} runs the data preparation step on its own and returns the
exact data the plot is drawn from. This helps with QC.
}
\examples{
plot_waterfall(
  tumor_change,
  subject = patient,
  value = best_change,
  fill = response
)

plot_waterfall(
  tumor_change,
  subject = patient,
  value = best_change,
  fill = response,
  tooltip = c(arm, age),
  engine = "plotly"
)

plot_waterfall(
  tumor_change,
  subject = patient,
  value = best_change,
  fill = arm,
  colors = c("Drug A" = "#0072B2", "Drug B" = "#E69F00"),
  engine = "echarts4r"
)

# Inspect the data behind the plot
prep_waterfall(tumor_change, subject = patient, value = best_change)
}
