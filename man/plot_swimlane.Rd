% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swimlane.R
\name{plot_swimlane}
\alias{plot_swimlane}
\alias{prep_swimlane}
\title{Swimlane plot}
\usage{
plot_swimlane(data, ..., engine = c("ggplot2", "plotly", "echarts4r"))

prep_swimlane(
  data,
  subject,
  end,
  start = NULL,
  fill = NULL,
  ongoing = NULL,
  tooltip = NULL,
  events = NULL,
  event_time = NULL,
  event = NULL,
  event_subject = NULL,
  sort = c("descending", "ascending", "none"),
  ref_lines = NULL,
  colors = NULL,
  event_colors = NULL,
  event_shapes = NULL,
  title = NULL,
  x_label = NULL,
  y_label = NULL
)
}
\arguments{
\item{data}{A data frame with one row per subject.}

\item{...}{Arguments passed on to \code{prep_swimlane()}.}

\item{engine}{Graphics engine: \code{"ggplot2"}, \code{"plotly"}, or \code{"echarts4r"}.}

\item{subject}{<\code{\link[tidyselect:language]{tidy-select}}> Column that
identifies each subject. Must not contain missing values.}

\item{end}{<\code{\link[tidyselect:language]{tidy-select}}> Numeric column with the
time each lane ends. Rows where it is missing are dropped with a warning.}

\item{start}{<\code{\link[tidyselect:language]{tidy-select}}> Optional numeric column
with the time each lane starts. Lanes start at 0 if it is not given.}

\item{fill}{<\code{\link[tidyselect:language]{tidy-select}}> Optional column used to
color the lanes, for example the treatment arm. Factor level order is
kept; missing values are shown as \code{"Missing"}.}

\item{ongoing}{<\code{\link[tidyselect:language]{tidy-select}}> Optional column
flagging subjects who are still ongoing; their lanes end with an
\code{"Ongoing"} arrow marker. Either logical, or character where \code{"Y"} means
ongoing (as in CDISC flags). Missing values count as not ongoing.}

\item{tooltip}{<\code{\link[tidyselect:language]{tidy-select}}> Optional extra columns
of \code{data} shown in the lane tooltip. Tooltips are only used by interactive
engines.}

\item{events}{Optional data frame of events to draw as markers.}

\item{event_time}{<\code{\link[tidyselect:language]{tidy-select}}> Numeric column of
\code{events} with the time of each event. Rows where it is missing are dropped
with a warning. Required when \code{events} is given.}

\item{event}{<\code{\link[tidyselect:language]{tidy-select}}> Column of \code{events} with
the event type, used for both marker shape and color. Required when
\code{events} is given.}

\item{event_subject}{<\code{\link[tidyselect:language]{tidy-select}}> Column of
\code{events} that identifies the subject. Defaults to the column with the same
name as \code{subject}. Events for subjects not in \code{data} are dropped with a
warning.}

\item{sort}{How to order the lanes from top to bottom: \code{"descending"}
(longest lane at the top, the default), \code{"ascending"}, or \code{"none"} (keep
the row order of \code{data}).}

\item{ref_lines}{Optional numeric vector of times for dashed vertical
reference lines.}

\item{colors}{Optional colors for the \code{fill} levels: either a named
character vector (names are levels) or an unnamed one used in level order.}

\item{event_colors}{Optional colors for the \code{event} levels, given the same
way as \code{colors}.}

\item{event_shapes}{Optional shapes for the \code{event} levels, given the same
way as \code{colors}. Use \code{"circle"}, \code{"square"}, \code{"triangle"}, \code{"diamond"},
\code{"cross"}, \code{"star"}, or \code{"arrow"}.}

\item{title}{Optional plot title.}

\item{x_label, y_label}{Optional axis titles. By default the \code{label}
attribute of the \code{end} and \code{subject} columns is used, or the column name
if there is none.}
}
\value{
\itemize{
\item \code{plot_swimlane()}: the engine's own object, a \code{ggplot} for ggplot2 or an
htmlwidget for plotly and echarts4r, which you can keep customizing.
\item \code{prep_swimlane()}: a \code{floaties_swimlane_spec} object. Its \code{data} element
has one row per subject in plot order (top to bottom), with columns
\code{.subject}, \code{.start}, \code{.end}, \code{.fill}, \code{.ongoing}, and \code{.tooltip}. Its
\code{events} element has columns \code{.subject}, \code{.time}, \code{.event}, and
\code{.tooltip}, including one \code{"Ongoing"} row at the end of each ongoing
lane, or is \code{NULL}.
}
}
\description{
Draw a swimlane plot: one horizontal lane per subject, for example time on
treatment, with optional markers for events such as responses,
progression, or death.

\code{plot_swimlane()} works with any data standard. It takes two data frames:
\itemize{
\item \code{data} has \strong{one row per subject} and describes the lanes.
\item \code{events} (optional) has any number of rows per subject and describes the
markers.
}

All times must be numeric and in the same unit (for example days or months
from the start of treatment). Derive them before calling the function.

\code{prep_swimlane()} runs the data preparation step on its own and returns the
exact data the plot is drawn from. This helps with QC.
}
\examples{
plot_swimlane(
  treatment_duration,
  subject = patient,
  end = months,
  fill = arm,
  ongoing = ongoing,
  events = response_events,
  event_time = month,
  event = event
)

plot_swimlane(
  treatment_duration,
  subject = patient,
  end = months,
  fill = arm,
  ongoing = ongoing,
  events = response_events,
  event_time = month,
  event = event,
  event_shapes = c(
    CR = "star", PR = "triangle", PD = "square", Death = "cross"
  ),
  engine = "plotly"
)

plot_swimlane(
  treatment_duration,
  subject = patient,
  end = months,
  ongoing = ongoing,
  events = response_events,
  event_time = month,
  event = event,
  engine = "echarts4r"
)

# Inspect the data behind the plot
prep_swimlane(treatment_duration, subject = patient, end = months)
}
