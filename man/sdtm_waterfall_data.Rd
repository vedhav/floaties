% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdtm_waterfall_data.R
\name{sdtm_waterfall_data}
\alias{sdtm_waterfall_data}
\title{Derive waterfall plot data from SDTM tumor datasets}
\usage{
sdtm_waterfall_data(
  tr,
  rs = NULL,
  dm = NULL,
  evaluator = "INVESTIGATOR",
  baseline_visit = NULL
)
}
\arguments{
\item{tr}{SDTM TR (Tumor Results) data frame, e.g.
\code{pharmaversesdtm::tr_onco}.}

\item{rs}{Optional SDTM RS (Disease Response) data frame. When given, the
best overall response (\code{RSTESTCD == "OVRLRESP"}) is added as \code{BOR}.}

\item{dm}{Optional SDTM DM (Demographics) data frame. When given, the
actual treatment arm is added as \code{ACTARM}.}

\item{evaluator}{Value of \code{TREVAL}/\code{RSEVAL} to use, such as
\code{"INVESTIGATOR"} or \code{"INDEPENDENT ASSESSOR"}. Where an evaluator has
several readers, only records flagged as accepted (\code{--ACPTFL == "Y"})
are kept.}

\item{baseline_visit}{Optional \code{VISIT} value that marks baseline, such as
\code{"BASELINE"} or \code{"SCREENING"}.}
}
\value{
A data frame with one row per subject and columns \code{USUBJID},
\code{BASE} (baseline sum of diameters), \code{PCHG} (best percent change from
baseline), and, when \code{rs}/\code{dm} are supplied, \code{BOR} (a factor ordered
\code{CR}, \code{PR}, \code{SD}, \code{NON-CR/NON-PD}, \code{PD}, \code{NE}) and \code{ACTARM}. \code{BOR} is
the best response recorded at any visit; it does not apply RECIST
confirmation rules.
}
\description{
Turns SDTM tumor results (TR), and optionally disease response (RS) and
demographics (DM), into one row per subject with the best percent change
from baseline in the sum of target lesion diameters. The result can be
passed straight to \code{\link[=waterfall_plot]{waterfall_plot()}}.
}
\details{
The sum of diameters is taken from \code{TRTESTCD == "SUMDIAM"} records when
\code{tr} has them. Otherwise it is summed from the target lesion longest
diameters (\code{TRTESTCD == "LDIAM"}), and a visit counts only when every
target lesion measured at baseline was measured again.

Baseline is the subject's earliest visit (lowest \code{VISITNUM}) unless
\code{baseline_visit} is given. The best percent change is the smallest
(most negative) change across all post-baseline visits. Subjects without
target lesions, a usable baseline, or any post-baseline assessment are
dropped with a message.
}
\examples{
\dontshow{if (requireNamespace("pharmaversesdtm", quietly = TRUE)) withAutoprint(\{ # examplesIf}
wf <- sdtm_waterfall_data(
  pharmaversesdtm::tr_onco,
  rs = pharmaversesdtm::rs_onco,
  dm = pharmaversesdtm::dm
)
head(wf)

waterfall_plot(wf, USUBJID, PCHG, fill = BOR)
\dontshow{\}) # examplesIf}
}
