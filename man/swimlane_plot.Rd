% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swimlane_plot.R
\name{swimlane_plot}
\alias{swimlane_plot}
\title{Swimlane plot of time on treatment and events}
\usage{
swimlane_plot(
  data,
  id,
  duration,
  time = NULL,
  event = NULL,
  fill = NULL,
  xlab = "Time since first dose",
  ylab = "Subject",
  title = NULL,
  engine = c("ggplot2", "plotly", "echarts4r")
)
}
\arguments{
\item{data}{A data frame, see Details.}

\item{id}{<\code{\link[ggplot2:aes_eval]{data-masking}}> Column that identifies
subjects.}

\item{duration}{<\code{\link[ggplot2:aes_eval]{data-masking}}> Numeric column with
the length of each subject's lane. Must be the same on every row of a
subject. Subjects with a missing value are dropped with a warning.}

\item{time, event}{<\code{\link[ggplot2:aes_eval]{data-masking}}> Optional columns
with the time and type of each event. Supply both or neither.}

\item{fill}{<\code{\link[ggplot2:aes_eval]{data-masking}}> Optional categorical
column used to color the lanes, such as treatment arm.}

\item{xlab, ylab, title}{Axis labels and plot title.}

\item{engine}{Plotting package to use: \code{"ggplot2"}, \code{"plotly"}, or
\code{"echarts4r"}. The interactive engines need their package installed.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object for \code{engine = "ggplot2"}, otherwise a
plotly or echarts4r htmlwidget. Each can be customized further with its
own package's functions.
}
\description{
Draws a swimlane plot: one horizontal bar (lane) per subject showing time
on treatment, sorted with the longest at the top, and points marking
events such as tumor response assessments along each lane.
}
\details{
\code{data} is in long format: one row per event, repeating the subject's
\code{duration} (and \code{fill}) on each row. Subjects without events have a single
row with \code{time} and \code{event} set to \code{NA}. \code{\link[=sdtm_swimlane_data]{sdtm_swimlane_data()}} returns
data in this shape.

When every event is a RECIST response (\code{CR}, \code{PR}, \code{SD}, \code{NON-CR/NON-PD},
\code{PD}, \code{NE}) or \code{Death}, standard colors are used: green for response,
red for progression, and a cross for death.

The plot can be drawn with ggplot2 (static, the default), plotly, or
echarts4r. The interactive engines show lane and event details on hover
and use the same colours as the ggplot2 version.
}
\examples{
events <- data.frame(
  subject = c("S1", "S1", "S1", "S2", "S2", "S3"),
  weeks = c(24, 24, 24, 18, 18, 12),
  arm = c("A", "A", "A", "B", "B", "A"),
  time = c(6, 12, 18, 6, 12, NA),
  response = c("SD", "PR", "CR", "SD", "PD", NA)
)

swimlane_plot(events, subject, weeks, time, response, fill = arm)

\dontshow{if (requireNamespace("plotly", quietly = TRUE)) withAutoprint(\{ # examplesIf}
swimlane_plot(events, subject, weeks, time, response, fill = arm, engine = "plotly")
\dontshow{\}) # examplesIf}
\dontshow{if (requireNamespace("echarts4r", quietly = TRUE)) withAutoprint(\{ # examplesIf}
swimlane_plot(events, subject, weeks, time, response, fill = arm, engine = "echarts4r")
\dontshow{\}) # examplesIf}
}
