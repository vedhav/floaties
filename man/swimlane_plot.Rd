% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swimlane.R
\name{swimlane_plot}
\alias{swimlane_plot}
\alias{swimlane_ggplot}
\alias{swimlane_plotly}
\alias{swimlane_echarts4r}
\title{Swimlane plot}
\usage{
swimlane_plot(data, ..., engine = getOption("floaties.engine", "ggplot2"))

swimlane_ggplot(
  data,
  id,
  start,
  end,
  color = NULL,
  ongoing = NULL,
  events = NULL,
  event_time = NULL,
  event_type = NULL,
  sort = TRUE,
  title = NULL,
  x_label = "Time",
  y_label = "Subject"
)

swimlane_plotly(
  data,
  id,
  start,
  end,
  color = NULL,
  ongoing = NULL,
  events = NULL,
  event_time = NULL,
  event_type = NULL,
  sort = TRUE,
  title = NULL,
  x_label = "Time",
  y_label = "Subject"
)

swimlane_echarts4r(
  data,
  id,
  start,
  end,
  color = NULL,
  ongoing = NULL,
  events = NULL,
  event_time = NULL,
  event_type = NULL,
  sort = TRUE,
  title = NULL,
  x_label = "Time",
  y_label = "Subject"
)
}
\arguments{
\item{data}{A data frame with one row per subject.}

\item{...}{Arguments passed on to the engine's generator.}

\item{engine}{Plotting engine: one of \code{"ggplot2"}, \code{"plotly"} or
\code{"echarts4r"}. Defaults to the \code{floaties.engine} option, or \code{"ggplot2"}
if that is unset.}

\item{id}{Name of the subject identifier column. The same column name is
used to match \code{events} to lanes.}

\item{start, end}{Names of the numeric columns holding the start and end of
each lane.}

\item{color}{Optional name of a column used to colour lanes, such as the
treatment arm.}

\item{ongoing}{Optional name of a logical column; \code{TRUE} marks subjects
still on study, drawn with an arrow at the end of their lane.}

\item{events}{Optional data frame of events, one row per event, containing
the \code{id}, \code{event_time} and \code{event_type} columns.}

\item{event_time, event_type}{Names of the event time (numeric) and event
type columns in \code{events}. Required when \code{events} is supplied.}

\item{sort}{If \code{TRUE}, lanes are ordered by duration with the longest at
the top. If \code{FALSE}, lanes keep the row order of \code{data}, top to bottom.}

\item{title}{Optional plot title.}

\item{x_label, y_label}{Axis titles.}
}
\value{
A ggplot object, or a plotly or echarts4r htmlwidget, depending on
the engine.
}
\description{
Draws one horizontal lane per subject spanning its time on study, with
optional markers for events (e.g. responses, dose changes) and arrows for
subjects who are still ongoing.
}
\details{
\code{swimlane_plot()} dispatches to the generator for the chosen \code{engine}. The
generators can also be called directly: \code{swimlane_ggplot()} returns a
ggplot object, \code{swimlane_plotly()} a plotly htmlwidget and
\code{swimlane_echarts4r()} an echarts4r htmlwidget. All three take the same
arguments and draw the same plot.
}
\examples{
lanes <- data.frame(
  subject = c("S01", "S02", "S03", "S04"),
  start = 0,
  end = c(120, 85, 200, 45),
  arm = c("A", "B", "A", "B"),
  on_study = c(TRUE, FALSE, TRUE, FALSE)
)
events <- data.frame(
  subject = c("S01", "S01", "S02", "S03", "S04"),
  day = c(30, 90, 60, 150, 40),
  response = c("PR", "CR", "PD", "PR", "PD")
)

swimlane_plot(
  lanes,
  id = "subject", start = "start", end = "end",
  color = "arm", ongoing = "on_study",
  events = events, event_time = "day", event_type = "response"
)

if (requireNamespace("plotly", quietly = TRUE)) {
  swimlane_plot(
    lanes,
    id = "subject", start = "start", end = "end", color = "arm",
    engine = "plotly"
  )
}
}
