% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adam_waterfall_data.R
\name{adam_waterfall_data}
\alias{adam_waterfall_data}
\title{Derive waterfall plot data from ADaM tumor datasets}
\usage{
adam_waterfall_data(
  adtr,
  adrs = NULL,
  adsl = NULL,
  param = "SDIAM",
  bor_param = "BOR",
  arm = "TRT01A",
  anl_flag = "ANL01FL"
)
}
\arguments{
\item{adtr}{ADaM ADTR data frame, e.g. \code{pharmaverseadam::adtr_onco}.}

\item{adrs}{Optional ADaM ADRS data frame, e.g.
\code{pharmaverseadam::adrs_onco}. When given, the best overall response is
added as \code{BOR}.}

\item{adsl}{Optional ADaM ADSL data frame, e.g. \code{pharmaverseadam::adsl}.
When given, the treatment arm is added.}

\item{param}{\code{PARAMCD} of the sum of diameters in \code{adtr}.}

\item{bor_param}{\code{PARAMCD} of the best overall response in \code{adrs}. Use
\code{"CBOR"} for the confirmed best overall response where the study has it.}

\item{arm}{Name of the treatment arm column in \code{adsl}.}

\item{anl_flag}{Name of the analysis flag column in \code{adtr}; only records
where it is \code{"Y"} are used. In the admiral ADTR template \code{ANL01FL}
excludes visits where not every target lesion was assessed, whose sums
of diameters would overstate shrinkage. Use \code{NULL} to use all records.}
}
\value{
A data frame with one row per subject and columns \code{USUBJID},
\code{BASE}, \code{PCHG} (best percent change from baseline), and, when
\code{adrs}/\code{adsl} are supplied, \code{BOR} (a factor ordered \code{CR}, \code{PR}, \code{SD},
\code{NON-CR/NON-PD}, \code{PD}, \code{NE}, with other values such as \code{"MISSING"} set
to \code{NA}) and the \code{arm} column.
}
\description{
Turns an ADaM tumor results dataset (ADTR), and optionally tumor response
(ADRS) and subject-level (ADSL) datasets, into one row per subject with the
best percent change from baseline. The result can be passed straight to
\code{\link[=waterfall_plot]{waterfall_plot()}}.
}
\details{
Unlike \code{\link[=sdtm_waterfall_data]{sdtm_waterfall_data()}}, nothing is derived from raw measurements:
the percent change (\code{PCHG}) and baseline (\code{BASE}, \code{ABLFL}) come from ADTR,
and the best overall response is read from its ADRS parameter. The best
percent change is the smallest (most negative) \code{PCHG} across post-baseline
records. Subjects without any post-baseline \code{PCHG} are dropped with a
message.
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE)) withAutoprint(\{ # examplesIf}
wf <- adam_waterfall_data(
  pharmaverseadam::adtr_onco,
  adrs = pharmaverseadam::adrs_onco,
  adsl = pharmaverseadam::adsl
)
wf

waterfall_plot(wf, USUBJID, PCHG, fill = BOR)
\dontshow{\}) # examplesIf}
}
