% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adam_swimlane_data.R
\name{adam_swimlane_data}
\alias{adam_swimlane_data}
\title{Derive swimlane plot data from ADaM datasets}
\usage{
adam_swimlane_data(
  adsl,
  adrs = NULL,
  param = "OVR",
  arm = "TRT01A",
  anl_flag = "ANL01FL",
  time_unit = c("days", "weeks", "months")
)
}
\arguments{
\item{adsl}{ADaM ADSL data frame, e.g. \code{pharmaverseadam::adsl}.}

\item{adrs}{Optional ADaM ADRS data frame, e.g.
\code{pharmaverseadam::adrs_onco}. When given, records of the \code{param}
parameter become events and only subjects with such records are kept.}

\item{param}{\code{PARAMCD} of the per-visit overall response in \code{adrs}.}

\item{arm}{Name of the treatment arm column in \code{adsl}.}

\item{anl_flag}{Name of the analysis flag column in \code{adrs}; only records
where it is \code{"Y"} are used. Use \code{NULL} to use all records.}

\item{time_unit}{Unit for \code{DURATION} and \code{TIME}: \code{"days"}, \code{"weeks"}, or
\code{"months"} (30.4375 days).}
}
\value{
A data frame with columns \code{USUBJID}, the \code{arm} column, \code{DURATION}
(time on treatment), \code{TIME} (time of the event), and \code{EVENT} (a factor
of response categories plus \code{"Death"}). Subjects without events have one
row with \code{TIME} and \code{EVENT} set to \code{NA}.
}
\description{
Turns an ADaM subject-level dataset (ADSL) and, optionally, tumor response
(ADRS) into the long format used by \code{\link[=swimlane_plot]{swimlane_plot()}}: each subject's time
on treatment plus one row per response assessment and death.
}
\details{
Time on treatment runs from \code{TRTSDT} to \code{TRTEDT}. Event times are counted
from \code{TRTSDT} like \code{ADY}: day 1 is the day of first dose and there is no
day 0. Response assessments are dated by \code{ADT} and deaths by \code{DTHDT}.
Records with missing dates are dropped with a message.
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE)) withAutoprint(\{ # examplesIf}
sw <- adam_swimlane_data(
  pharmaverseadam::adsl,
  adrs = pharmaverseadam::adrs_onco,
  time_unit = "weeks"
)
sw

swimlane_plot(
  sw,
  USUBJID,
  DURATION,
  TIME,
  EVENT,
  fill = TRT01A,
  xlab = "Weeks since first dose"
)
\dontshow{\}) # examplesIf}
}
