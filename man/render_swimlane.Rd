% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swimlane-echarts4r.R, R/swimlane-ggplot2.R,
%   R/swimlane-plotly.R, R/swimlane.R
\name{render_swimlane.floaties_echarts4r}
\alias{render_swimlane.floaties_echarts4r}
\alias{render_swimlane.floaties_ggplot2}
\alias{render_swimlane.floaties_plotly}
\alias{render_swimlane}
\title{Render a prepared swimlane}
\usage{
\method{render_swimlane}{floaties_echarts4r}(engine, x, ...)

\method{render_swimlane}{floaties_ggplot2}(engine, x, ...)

\method{render_swimlane}{floaties_plotly}(engine, x, ...)

render_swimlane(engine, x, ...)
}
\arguments{
\item{engine}{A \link[=engines]{floaties engine}; the object dispatched on.}

\item{x}{Canonical data from \code{\link[=prepare_swimlane]{prepare_swimlane()}}.}

\item{...}{Passed to methods.}
}
\value{
The engine's own plot object.
}
\description{
The engine-specific half of the pipeline. Write methods for this to add an
engine; nothing else has to change.
}
\details{
The echarts4r method draws lanes on a category y axis and time on a value x
axis. Intervals become line segments separated by \code{NA}, which is what lets
overlapping intervals on one lane render correctly, and point events become
a scatter series. Unlike the ggplot2 and plotly methods it does not colour by
\code{.type}, because doing so needs one series per type and the legend stops
matching the other engines. Treat that as a known difference rather than a
bug, and eyeball the result: no test can tell you a swimlane reads correctly.

Methods receive canonical data and may rely on the contract enforced by
\code{\link[=validate_swimlane_data]{validate_swimlane_data()}}: \code{.id} is a factor in lane order, \code{.start} is
numeric and complete, \code{.end} is numeric and \code{NA} for point events, \code{.type} is
character and may be all \code{NA}.
}
\examples{
pd <- prepare_swimlane(data.frame(s = "a", d = 1, e = 9), s, d, e)
render_swimlane(eng_ggplot2(), pd)

}
