% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waterfall.R
\name{waterfall}
\alias{waterfall}
\title{Waterfall plot}
\usage{
waterfall(
  data,
  x_var,
  value_var,
  sort_var = NULL,
  color_var = NULL,
  hlines = NULL,
  title = NULL,
  engine = eng_ggplot2()
)
}
\arguments{
\item{data}{A data frame, one row per subject.}

\item{x_var}{Column identifying the subject. One bar each.}

\item{value_var}{Numeric column giving the bar height.}

\item{sort_var}{Column to sort by. The default, \code{NULL}, sorts by
\code{value_var} descending, because a waterfall that is not sorted by value is
not a waterfall. Pass \code{FALSE} to keep the order of the input.}

\item{color_var}{Optional column driving the fill colour, e.g. a response
category.}

\item{hlines}{Numeric vector of reference line positions. These are in the
units of \code{value_var}: for RECIST against a percentage change column that is
\code{c(20, -30)}, not \code{c(0.2, -0.3)}.}

\item{title}{Plot title, or \code{NULL} for none.}

\item{engine}{A \link[=engines]{floaties engine}.}
}
\value{
The engine's own plot object: a \code{ggplot}, a \code{plotly} or an
\code{echarts4r}. You can keep customising it with that library's own verbs.
}
\description{
One bar per subject, sorted worst to best. The chart a medical monitor uses
to see at a glance who responded and by how much.
}
\details{
Column roles are arguments, so it does not matter where the data came from
as long as it has one row per subject with a value to plot. Deriving that
value, for example a best percentage change from baseline, is the caller's
job.
}
\examples{
subjects <- data.frame(
  id       = sprintf("01-\%03d", 1:8),
  change   = c(-100, -62, -41, -30, -8, 12, 24, 57),
  response = c("CR", "PR", "PR", "PR", "SD", "SD", "PD", "PD")
)

waterfall(subjects, id, change)
waterfall(subjects, id, change, color_var = response, hlines = c(20, -30))

}
\seealso{
\code{\link[=swimlane]{swimlane()}}
}
