# Prompts used in Claude Opus 5.5 (1M context) with medium effort thinking
I want to create an R package called floaties. Can you set up the basic package structure for me so I can start working on it?
Can you create a function to create a waterfall plot in my package?
I want to modify the waterfall plot so it works on SDTM datasets. Particularly, I want to use the SDTM datasets from the `pharmaversesdtm` package.
Now, I want a swimlane plot creation function that also works with SDTM datasets from pharmaversesdtm.
I have a new requirement from the users of the package. They would like to have plotly and echarts4r variants of the plots for interactivity. I would still also have the ggplot2 variant for backwards compatibility. Can you implement this for both the plots we have?
We have some new users who would like to work on ADaM datasets instead of SDTM so can you make sure that our plots work when we use ADaM datasets from the pharmaverseadam package?
